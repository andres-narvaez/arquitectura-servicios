{
  "name": "arquitectura-servicios",
  "private": true,
  "packageManager": "pnpm@8.15.7",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:apps": "turbo run dev --filter=liberty-fund --filter=board --filter=honoraria",
    "dev:storybook": "turbo run dev --filter=storybook",
    "build": "turbo run build",
    "start": "turbo run start",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "^1.8.8"
  }
}
